---
- name: SEND REBOOT EMAIL
  hosts: all
  
  tasks:
    - mail:
        host: mail.nortonhealthcare.org
        port: 25
        to: SDE
        subject: RSL
        body: [ "{{ hostname }}" ]  <Emergency Reboot>
      delegate_to: localhost
